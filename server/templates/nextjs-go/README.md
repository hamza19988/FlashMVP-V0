# nextjs-go starter template

Owner: Person 4 (BL-ARC-01). This folder is copied into `WORKSPACE_DIR/<project_id>/`
when a project is scaffolded.

To do: add the starter source, `Dockerfile.frontend` and `Dockerfile.backend`
referenced by `flashmvp.json`.
